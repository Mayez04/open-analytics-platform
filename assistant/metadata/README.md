# AI Assistant — Metadata

YAML/JSON metadata files describing analytical databases for the AI assistant (Phase 5).

Each dataset gets a metadata file with table descriptions, synonyms, relationships, and example questions.
